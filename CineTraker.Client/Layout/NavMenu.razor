@inject IJSRuntime JS
@using Microsoft.AspNetCore.Components.Authorization

<div class="menu-pill-container @(collapseNavMenu ? "closed" : "open")"
     @onmouseover="ExpandMenu"
     @onmouseout="CollapseMenu">

    @* Envolvemos las opciones para manejar la transición de visibilidad *@
    <div class="menu-options">
        <AuthorizeView>
            <Authorized>
                <NavLink class="nav-btn circular-btn" href="logout" title="Cerrar Sesión">
                    <i class="bi bi-box-arrow-right"></i>
                </NavLink>
                <NavLink class="nav-btn circular-btn" href="mis-mapas" title="Mis Expediciones">
                    <i class="bi bi-map-fill"></i>
                </NavLink>
                <NavLink class="nav-btn circular-btn" href="profile" title="Mi Perfil">
                    <i class="bi bi-person-circle"></i>
                </NavLink>
            </Authorized>
            <NotAuthorized>
                <NavLink class="nav-btn circular-btn" href="login" title="Login">
                    <i class="bi bi-person-badge-fill"></i>
                </NavLink>
            </NotAuthorized>
        </AuthorizeView>


        <NavLink class="nav-btn circular-btn" href="" Match="NavLinkMatch.All" title="Inicio">
            <i class="bi bi-house-door-fill"></i>
        </NavLink>
    </div>

    <button class="main-trigger" @onclick="ToggleNavMenu">
        <i class="bi bi-list"></i>
    </button>
</div>

@code {
    private bool collapseNavMenu = true;
    private void ToggleNavMenu() => collapseNavMenu = !collapseNavMenu;
    private void ExpandMenu() => collapseNavMenu = false;
    private void CollapseMenu() => collapseNavMenu = true;
}